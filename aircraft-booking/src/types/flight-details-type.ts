export type FlightDetails = {
    flightNumber: string
    airline: string
    boarding: string
    departureDateTime: string
    destination: string
    arrivalDateTime: string
    price: number
    seatsAvailable: number
}
